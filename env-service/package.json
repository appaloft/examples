{
  "name": "appaloft-example-env-service",
  "version": "1.0.0",
  "private": true,
  "description": "Env-injection demo for Appaloft (non-secret config only)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
