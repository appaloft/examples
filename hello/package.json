{
  "name": "appaloft-example-hello",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Appaloft hello example (appaloft/examples/hello): public Git → container deploy with health checks",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
